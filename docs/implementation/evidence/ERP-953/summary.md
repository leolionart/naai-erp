# ERP-953 summary

Added a first-party cross-environment clone workflow. The CLI orchestrates source export/download and target restore through versioned REST, while the target API accepts an explicit source organization and remaps organization identity. Credentials remain process-only.
