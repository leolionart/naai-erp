# ERP-907 acceptance

## Acceptance status

| Criterion                                                         | Current proof                                                                                                                                                                                                                               | Status                                             |
| ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| Eligible solopreneur owner has one save-and-record action         | Commercial-document and expense stores detect persisted solopreneur mode plus owner role, complete eligible lifecycle/posting in one transaction, and return the posted result; UI capability policy labels the action **Lưu và ghi nhận**. | Passed API integration and UI policy tests         |
| Controlled mode retains approval separation                       | Auto-completion is restricted to solopreneur owner capability. Financial-report mapping selection remains approved-only in controlled mode, with a focused regression assertion.                                                            | Passed DB regression                               |
| Posted accounting safeguards remain                               | Reporting queries still filter posted/reversed journals. Existing balanced posting, period-lock, duplicate, audit and idempotency services remain on the mutation path; posted correction behavior is not replaced by direct journal edits. | Implemented; full safeguard regression pending     |
| Canonical data appears without intermediate report approval gates | Dashboard and statements return posted canonical totals even when individual mappings/configuration are absent or unapproved in solopreneur mode. Missing configuration is returned in `reportWarnings` or `configurationWarnings`.         | Implemented; focused type/unit gate passed         |
| Missing data does not blank the whole report                      | A missing financial-statement mapping in solopreneur mode uses a canonical-unmapped fallback; ledger totals and unclassified sources remain visible rather than raising a report-wide not-found error.                                      | Passed DB regression                               |
| Tax eligibility is separate from accounting profit                | Expense/document defaults preserve management validity while VAT and CIT remain `unreviewed` unless an explicit tax decision exists. Posted expense remains in ledger-derived management profit.                                            | Passed DB regression                               |
| No manual stale/read-model refresh is required                    | Performance comparisons read canonical posted document, expense and banking facts directly. Migration 0053 drops the copied `planning_actual_facts` table.                                                                                  | Passed fresh/upgrade migration and DB integration  |
| Migration precedes new API startup                                | The supported Compose update path remains migrate-first and migration 0053 is registered in migration metadata.                                                                                                                             | Implemented in release path; Compose smoke pending |

All automated repository gates passed. The product owner explicitly accepted the rendered UI and
requested that additional manual UI testing be skipped.
