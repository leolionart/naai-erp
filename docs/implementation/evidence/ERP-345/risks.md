# ERP-345 Risks

- Legacy handcrafted CSS remains temporarily for the existing operational workspace layout; controls and new shell primitives now use semantic components/tokens and the stylesheet can shrink incrementally.
- Dense accounting tables require server pagination and measured virtualization in later modules.
- The Browser plugin was unavailable during QA, so local rendered evidence used Playwright Chromium instead.
