# Acceptance

- Expense/revenue rows default to document/activity date descending.
- Supported headers expose ascending/descending controls without losing the current list.
- Activity log pagination uses the API cursor and appends unseen rows instead of replacing them.
- Refresh remains explicit; automatic polling no longer resets the viewer's scroll/list.
