# ERP-933 risks

- Historical projects cannot show revenue until source transactions receive reviewed canonical
  project relationships or recognition events. Amount/date/customer similarity is insufficient for an
  automatic mapping.
- The directory shows lifetime-to-cutoff progress for projects overlapping the selected period; the
  period selects projects and does not rewrite contract progress into an annual-only amount.
- No database migration is required.
