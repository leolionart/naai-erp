# ERP-869 acceptance

- Current unclosed loss is included in accumulated loss from the canonical Balance Sheet; production
  readback is `486,348,287` VND minor units.
- Missing real contributed capital remains an explicit non-positive denominator, not a generated value.
- Standard operating cash counterpart accounts are configured in an approved production mapping.
- Owner-current balance uses a non-misleading label.
- Clearing-account transactions remain reviewable until their business purpose is supported.
- Profitability renders Gross margin `100%`, Operating margin / Net margin / ROS `-20.41%` from
  production records.
- Liquidity renders unrestricted cash `78,333,660` VND; burn and runway remain explicit N/A because
  reviewed operating burn is unavailable while clearing movements are unresolved.
- Returns renders ROA `-42.84%`; ROE remains explicit N/A because average equity is non-positive.
- ROI remains explicitly unconfigured instead of fabricating a cross-purpose numerator or denominator.
- Every rendered exact metric retains a source drill-down with formula, cutoff and ledger fingerprint.
