## Acceptance

- Google button is visible and named `Đăng nhập với Google` — verified by Playwright.
- Google button is disabled and status text says `Sắp ra mắt` — verified by Playwright.
- Existing login succeeds and redirects to `/dashboard` — verified by Playwright.
- No Google OAuth/API behavior was added — confirmed by UI-only diff.
