# Acceptance

- Cards show value, descriptive text, status and trend where monthly source data exists — implemented.
- Trend chart uses `ChartContainer` and semantic chart token — implemented.
- Translucent treatment remains accessible and uses component classes/tokens, not raw color CSS — implemented.
