# ERP-892 risks

- The sidebar contains more grouped destinations; labels and grouping should be reviewed again if
  the information architecture grows materially.
- E2E route mocks now match both direct API URLs and the local `/dev-api` proxy introduced by the
  native data-source profiles.
