# ERP-920 — Simple API-key and one-call ingestion

The automation screen now provides a masked, explicitly revealed API credential and copyable one-call
examples. Revenue ingestion resolves customer/project/category safely in one request; purchase
ingestion keeps the existing one-call supplier/category matching contract.
