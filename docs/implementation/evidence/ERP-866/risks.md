# ERP-866 risks

- Future server-side compose edits must remain synchronized with the checked-in compose contract;
  image-only updates do not automatically add newly introduced environment mappings.
- Login credentials remain server-only and were not recorded in evidence or command output.

