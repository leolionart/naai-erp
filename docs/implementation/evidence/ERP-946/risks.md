# Risks and follow-ups

- Posting fails closed with `OWNER_CURRENT_ACCOUNT_NOT_CONFIGURED` when the approved mapping is
  missing, ambiguous or inactive.
- Existing posted documents are unchanged; the default applies to new API writes and corrections
  that explicitly choose owner-paid funding.
