# Tests

- `pnpm --filter @naai-erp/web exec vitest run src/components/brand/naai-logo.test.tsx` — passed (2 tests).
- `pnpm --filter @naai-erp/web typecheck` — passed.
- `gpt-image-2` proxy generation — completed; generated mark visually inspected and resized to
  256px/512px web assets.
