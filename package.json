{
  "name": "naai-erp",
  "version": "0.0.0",
  "private": true,
  "description": "Management accounting and project profitability platform for NAAI Studio",
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.30.1",
  "engines": {
    "node": ">=22 <25",
    "pnpm": ">=10 <11"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "dev:preview": "turbo run dev --parallel --filter=@naai-erp/web --filter=@naai-erp/api",
    "cli": "pnpm --filter @naai-erp/cli dev --",
    "lint": "eslint . && turbo run lint",
    "format": "prettier --write \"**/*.{json,yaml,yml,ts,tsx,js,mjs,css}\"",
    "format:check": "prettier --check \"**/*.{json,yaml,yml,ts,tsx,js,mjs,css}\"",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:native-db": "vitest run tests/native-dev-db.test.ts",
    "test:docs": "node scripts/verify-docs.mjs",
    "test:security-baseline": "node scripts/verify-security-baseline.mjs",
    "test:fixtures": "node scripts/verify-golden-fixtures.mjs",
    "test:release": "node scripts/verify-release-workflow.mjs",
    "test:e2e": "pnpm --filter @naai-erp/web test:e2e",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test:docs && pnpm test:security-baseline && pnpm test:fixtures && pnpm test:native-db && pnpm test && pnpm build",
    "db:generate": "pnpm --filter @naai-erp/database db:generate",
    "db:migrate": "pnpm --filter @naai-erp/database db:migrate",
    "db:check": "pnpm --filter @naai-erp/database db:check",
    "db:native-setup": "node scripts/native-dev-db.mjs setup",
    "db:native-status": "node scripts/native-dev-db.mjs status",
    "seed:dev": "node scripts/seed-development.mjs",
    "prepare": "node scripts/setup-hooks.mjs",
    "clean": "turbo run clean && rm -rf .turbo node_modules"
  },
  "devDependencies": {
    "@eslint/js": "9.39.2",
    "eslint": "9.39.2",
    "globals": "17.9.0",
    "prettier": "3.9.6",
    "turbo": "2.10.8",
    "typescript": "5.9.3",
    "typescript-eslint": "8.66.0",
    "vitest": "4.1.10"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
